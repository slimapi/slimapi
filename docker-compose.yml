---
version: "3.7"
networks:
  app:
    driver: bridge

services:
  app:
    build: .

    volumes:
      - .:/var/www/:cached

    ports:
      - "80:80"

    networks:
      - app

    environment:
      TEST_ENV: test_env_val
      PHP_OPCACHE: 0
      PHP_PCOV: 1
